const List = (name) => {
    name 
    return { name }
};

const Task = (title, details, dateTime, position) => {
    title,
    details,
    dateTime,
    position

    return { title, details, dateTime, position };
};

let list1 = List('Weekend');
let task1 = Task('Dance', 'Samba', '2022-11-23T17:33', 1);
let task2 = Task('Sleep', 'Deep', '2022-11-26T11:11', 1);
